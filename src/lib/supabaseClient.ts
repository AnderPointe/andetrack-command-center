/**
 * Phase 2 — Convenience re-export so files outside `src/integrations/supabase/`
 * can `import { supabase } from "@/lib/supabaseClient"`.
 */
export { supabase } from "@/integrations/supabase/client";
